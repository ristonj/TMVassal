class TokenCommonData:
    def __init__(self, parent, child, tokenText, xStart, yStart):
        self.parent = parent
        self.child = child
        self.tokenText = tokenText
        self.xStart = xStart
        self.yStart = yStart